# CSwL

Tradução para Lean do livro CSwFP (Computational Semantics with Functional
Programming). Ver o `CLAUDE.md` do repositório pai (`emap-20262-nlp`) para o
contexto completo do curso e as convenções de conteúdo.

## Commits neste repositório

Este repositório é distribuído a alunos (via GitHub Classroom) e é público.

- **Nunca** adicionar `Co-Authored-By: Claude` (ou qualquer trailer de
  co-autoria do Claude) em mensagens de commit aqui.
- **Sempre** incluir um parágrafo de disclosure quando Claude tiver ajudado
  a produzir o conteúdo do commit (código, prosa, exercícios), no formato:

  ```
  ---
  Disclosure de uso de IA: <o que foi feito com auxílio de IA> foram
  desenvolvidas com auxílio de Claude <modelo> (Anthropic, <mês/ano>, via
  Claude Code). O autor revisou, testou (<como testou>) e assume total
  responsabilidade pelo conteúdo final.
  ```

  Ajustar o texto entre `<>` ao commit específico — não repetir um texto
  genérico sem relação com o que de fato mudou.
